# Environment variables for docker-compose and application configuration

# ========================================
# Server Configuration
# ========================================

# Your domain name (required)
# Example: DOMAIN=example.com
DOMAIN=your-domain.com

# Traefik ACME email for Let's Encrypt (required for SSL certificate generation)
# Example: TRAEFIK_ACME_EMAIL=your-email@example.com
TRAEFIK_ACME_EMAIL=your-email@example.com

# Server port (optional, defaults to 3000)
PORT=3000

# Rust logging level (optional, defaults to info)
RUST_LOG=info

# ========================================
# Database Configuration
# ========================================

# Database connection URL (required for database functionality)
# Format: postgresql://username:password@host:port/database_name
# Example: DATABASE_URL=postgresql://myuser:mypassword@localhost:5432/mydatabase
DATABASE_URL=postgresql://postgres:password@db:5432/rust_server_db

# Maximum number of database connections in the pool (optional, defaults to 10)
# Adjust based on your expected concurrent load
DB_MAX_CONNECTIONS=10

# Maximum lifetime of a database connection in seconds (optional, defaults to 3600 = 1 hour)
# Connections older than this will be closed and recreated
DB_MAX_LIFETIME=3600

# Idle timeout for database connections in seconds (optional, defaults to 600 = 10 minutes)
# Connections idle longer than this may be closed
DB_IDLE_TIMEOUT=600

# ========================================
# Docker Compose Database Service
# ========================================

# PostgreSQL configuration for docker-compose
POSTGRES_DB=rust_server_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# Note: In production, use strong passwords and consider using secrets management
# Example production DATABASE_URL:
# DATABASE_URL=postgresql://prod_user:strong_password@your-db-host:5432/production_db
